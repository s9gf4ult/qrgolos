<% if user_signed_in? %>
  <div id="formHeader">Мои мероприятия</div>

    <div id="mtng">
      <% @my_meetings.each do |meeting| %>
      <div id="mtng" class="name"><%= link_to meeting.name, meeting %></div>
      <div id="mtng" class="descr"><div><%= meeting.descr %></div>
	<div id="mtng" class="edit">
	  <%= link_to (t "buttons.edit"), edit_meeting_path(meeting), :class => "small_button" %>
	  <%= link_to (t "buttons.delete"), meeting, method: :delete, data: { confirm: 'Удалить ?' }, :class => "small_button"%>
	</div>
      </div>
      <% end %>
    </div>

<% end %>

<% if user_signed_in? %>
  <div id="formHeader">Все мерприятия</div>
<% else %>
  <div id="formHeader">Мероприятия</div>
<% end %>

<div class="meetings">
<% @meetings.each do |meeting| %>
    <div id="mtng" class="name"><%= link_to meeting.name, meeting %></div>
    <div id="mtng" class="descr"><%= meeting.descr %></div>
<% end %>
</div>

<hr>
<%= link_to (t "buttons.back"), meetings_path, :class => "button" %>
<%= link_to (t "buttons.create"), new_meeting_path, :class => "button", :style => "float: right;" %>