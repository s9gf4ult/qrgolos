
<% if user_signed_in? %>
<h2>Мои мероприятия</h2>
<table>
  <tr>
    <th>Name</th>
    <th>Descr</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @my_meetings.each do |meeting| %>
  <tr>
    <td><%= meeting.name %></td>
    <td><%= meeting.descr %></td>
    <td><%= meeting.user.email %></td>
    <td><%= link_to 'Смотреть', meeting %></td>
    <td><%= link_to 'Редактировать', edit_meeting_path(meeting) %></td>
    <td><%= link_to 'Удалить', meeting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Создать', new_meeting_path %>

<% end %>

<% if user_signed_in? %>
	<h2>Чужие мерприятия</h2>
<% else %>
	<h2>Мероприятия</h2>
<% end %>

<table>
  <tr>
    <th>Name</th>
    <th>Descr</th>
    <th>User</th>
  </tr>

<% @meetings.each do |meeting| %>
  <tr>
    <td><%= meeting.name %></td>
    <td><%= meeting.descr %></td>
    <td><%= meeting.user.email %></td>
  </tr>
<% end %>
</table>
